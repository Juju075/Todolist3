{% extends 'layouts/base.html.twig' %}{% block title %}Create{% endblock %} {%
block body %}
<div class="row">
  {{ form_start(form, { action: path("task_create") }) }}
  {{ form_widget(form) }}

  <button type="submit" class="btn btn-success pull-right">Submit</button>
  {{ form_end(form) }}

  <a href="{{ path('task_list') }}" class="btn btn-primary">Back to tasks</a>
</div>
{% endblock %}
