{% extends 'layouts/base.html.twig' %} {% block header_title %} {% endblock %}
{% block header_img %}{% endblock %} {% block body %}
<h1>Create an User</h1>
<div class="row">
  {{ form_start(form, { action: path("app_admin_user_create") }) }}
  {{ form_widget(form) }}

  <button type="submit" class="btn btn-success pull-right">Submit</button>
  {{ form_end(form) }}
</div>
{% endblock %}
