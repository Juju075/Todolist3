{% extends 'layouts/base.html.twig' %} {% block header_title %}
<h1>Create an User</h1>
{% endblock %} {% block header_img %}{% endblock %} {% block body %}
<div class="row">
  {{ form_start(form, { action: path("app_admin_user_create") }) }}
  {{ form_widget(form) }}

  <button type="submit" class="btn btn-success pull-right">Submit</button>
  {{ form_end(form) }}
</div>
{% endblock %}
