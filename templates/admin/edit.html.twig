{% extends 'layouts/base.html.twig' %} {% block header_title %}
<h1>
  Update <strong>{{ user.username }}</strong>
</h1>
{% endblock %} {% block header_img %}{% endblock %} {% block body %}
<div class="row">
  {{
    form_start(form, { action: path("app_admin_user_edit", { id: user.id }) })
  }}
  {{ form_widget(form) }}

  <button type="submit" class="btn btn-success pull-right">Update</button>
  {{ form_end(form) }}
</div>
{% endblock %}
